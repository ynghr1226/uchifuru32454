



<% if lesson.favorited_by?(current_user) %>  
   <%= link_to lesson_favorite_path(lesson.id), method: :delete, remote: true do %>
   
       <% lesson.favorites.count %>お気に入りに追加
   <% end %>

   <% else %>
      <%= link_to lesson_favorites_path(lesson.id), method: :post, remote: true do %>
         <% lesson.favorites.count %>お気に入りに追加
      <% end %>
<% end %>

